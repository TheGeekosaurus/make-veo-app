{
    "url": "/models/{{parameters.model}}:predictLongRunning",
    "method": "POST",
    "body": {},
    "response": {
        "output": {
            "operationName": "{{body.name}}"
        }
    }
}
