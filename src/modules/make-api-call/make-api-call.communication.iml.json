{
    "url": "{{parameters.url}}",
    "method": "{{parameters.method}}",
    "headers": "{{parameters.headers}}",
    "qs": "{{parameters.qs}}",
    "body": "{{parameters.body}}",
    "type": "{{parameters.bodyType}}",
    "response": {
        "output": "{{body}}"
    }
}
