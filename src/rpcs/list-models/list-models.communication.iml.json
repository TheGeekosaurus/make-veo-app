{
    "url": "/models",
    "method": "GET",
    "response": {
        "iterate": "{{body.models}}",
        "output": {
            "label": "{{item.displayName}}",
            "value": "{{item.name}}"
        }
    }
}
